// import { CMSProvider } from "@snek-at/jaen-cms"

// import * as React from "react"

// export const wrapRootElement = ({ element }) => {
//   return <CMSProvider templates={[]}>{element}</CMSProvider>
// }
