import {PageProps} from 'gatsby'
import React from 'react'

const basicTemplate = (_props: PageProps) => {
  return (
    <div style={{maxWidth: `960px`, margin: `1.45rem`}}>
      <h1>__dynamic</h1>
    </div>
  )
}
export default basicTemplate
